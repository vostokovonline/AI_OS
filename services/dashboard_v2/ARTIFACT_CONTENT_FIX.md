# Проблема с artifacts - АНАЛИЗ

## 🔍 Что обнаружено:

### 1. Структура Artifacts в БД:
- **Всего artifacts:** 68
- **Тип:** все FILE
- **Content_kind:** file
- **Content_location:** пути типа `/tmp/artifacts/файл.md`

### 2. Проблема:
- ❌ Файлы **не существуют** в `/tmp/artifacts/` внутри контейнера
- ❌ Dashboard v2 показывает только `content_location`, но не содержимое
- ❌ Нет API endpoint для чтения содержимого файлов

### 3. Причины:
1. Artifacts были зарегистрированы в БД через `artifact_registry.register_artifact()`
2. Но файлы не были созданы на диске
3. Или файлы были удалены при пересоздании контейнеров

## 💡 Решения:

### Вариант 1: Добавить endpoint для чтения файлов
```python
@app.get("/artifacts/{artifact_id}/content")
async def get_artifact_content(artifact_id: str):
    # Вернуть содержимое файла или сообщение что файла нет
```

### Вариант 2: Показывать что файла нет в UI
- Отобразить сообщение "File not found on disk"
- Показать только метаданные

### Вариант 3: Использовать хранилище содержимого в БД
- Хранить содержимое в таблице artifacts (колонка content)
- Не зависеть от файловой системы

## 🎯 Рекомендация:

Использовать **Вариант 3** - хранить содержимое в БД как JSON:
- FILE → содержимое файла в content
- KNOWLEDGE → текстовые знания в content
- REPORT → текст отчета в content

Это более надежно для Docker окружения.

## 📊 Текущий статус:

1. ✅ Artifacts регистрируются в БД
2. ✅ API возвращает метаданные
3. ❌ Содержимое файлов недоступно
4. ❌ Dashboard v2 не может показать содержимое
